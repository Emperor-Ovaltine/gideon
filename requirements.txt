py-cord==2.6.1          # Discord API framework (cogs + slash commands)
aiohttp==3.11.*          # Async HTTP for LLM/image provider clients + dashboard
python-dotenv==1.0.*     # .env loading
sympy==1.13.*            # Safe mathematical expression evaluation (calculate tool)
beautifulsoup4==4.12.*   # HTML parsing for URL summarization
openai==1.*              # OpenAI API access (chat + DALL-E)
cryptography==44.*       # Fernet encryption of API keys — keep current, gets CVEs
tzdata==2024.*         # IANA tz database for zoneinfo (slim images may lack system tzdata)
